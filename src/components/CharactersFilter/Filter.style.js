import styled from 'styled-components';

const StyledFilter = styled.div`
    padding: 20px 0;
    display: flex;
    flex-wrap: wrap;
    gap: 22px;

    .filter__select {
      padding: 12px 10px;
      border-radius: 4px;
  }
`;

export default StyledFilter;
